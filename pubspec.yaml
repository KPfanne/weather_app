name: weather_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  geocoding: ^5.0.0
  geolocator: ^14.0.3
  google_fonts: ^8.2.0
  http: ^1.6.0
  intl: ^0.20.3
  weather_icons_animated: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
